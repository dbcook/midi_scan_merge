# Ethernet MIDI aka Apple Midi aka RTP-MIDI Support

This package uses the [Apple Midi library](https://github.com/lathoub/Arduino-AppleMIDI-Library)
which works in conjunction with the standard Arduino Ethernet library and the 47Effects MIDI
library.

The README notes that you should enlarge the standard Arduino Ethernet library's buffer to avoid
dropping large SysEx messages.

